#include <iostream>
#include <vector>
#include <set>
using namespace std;

int main() {
    int a, b;
    cin >> a;
    cin >> b;
    int extent = a * b;
    cout << extent << endl;
    return 0;
}
